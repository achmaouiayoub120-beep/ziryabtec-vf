import { Font } from "@react-pdf/renderer";

export const FONTS = {
  display: "Helvetica",
  body: "Helvetica",
  mono: "Courier",
};

// No need to register standard fonts, they are built-in and 100% reliable!
export const registerFonts = () => {};
